Use shell scripts to add shortcuts to some commonly used long commands

## Usage
- add `./bin` to `PATH`

```
# vi ~/.bash_profile
add : PATH="/Users/apple/Desktop/program/shell/normal/bin:$PATH"
# source  ~/.bash_profile
```